# Forward .consul domain queries to Consul DNS
server=/consul/172.18.0.100#8600

# Forward all other queries to Google DNS
server=8.8.8.8

# Don't read /etc/resolv.conf
no-resolv

# Don't read /etc/hosts
no-hosts

# Log queries for debugging
log-queries
